---
description: RAG v2 — спринты, RETRO, Bugbot-review, backlog оптимизаций
alwaysApply: true
---

# RAG v2 Migration

## Старт чата (без запроса у пользователя)

1. `docs/sprints/SPRINT_STATE.md` — current_sprint, RETRO, блокеры
2. `docs/architecture/RAG_MIGRATION_PLAN.md` §6, §11, §12 (открытые OPT)
3. `git log` + код — что уже сделано
4. Продолжить незавершённые задачи текущего спринта

Не просить пользователя называть спринт или пересказывать план.

## Когда спрашивать пользователя

- противоречие код / STATE / §6 / §11;
- решение вне §11;
- scope change;
- OPT типа `scope` или меняющие §11;
- Bugbot critical/high без очевидного фикса в рамках спринта.

## Во время спринта

- Задачи и DoD — `RAG_MIGRATION_PLAN.md` §6
- §11 не менять без ответа пользователя
- Коммиты: `S{N}:`

## Конец спринта (обязательный порядок)

1. **Тесты** по задачам спринта
2. **Bugbot-review** (обязательно после каждого спринта):
   - Запустить subagent `bugbot` (`readonly: true`)
   - Prompt: `Full Repository Path: <abs path avgexpert>\nDiff: branch changes`
   - Сравнивать коммиты спринта с merge-base `main`
   - Итог (findings / no issues) — в RETRO и таблицу Bugbot
   - Critical/high: исправить в рамках спринта или эскалировать пользователю
3. **RETRO** — блок `### RETRO S{N}` в `SPRINT_STATE.md` (шаблон в файле)
4. **OPT** — предложения в `RAG_MIGRATION_PLAN.md` §12 (`proposed`)
5. **commit + push** (включая STATE/§12 если менялись)
6. Краткий отчёт пользователю
7. `current_sprint` → следующий; обновить таблицу прогресса задач
8. Не начинать следующий спринт без запроса

## Применение OPT

После ответа пользователя (`ок OPT-xxx`):
- `approved` → правка §4–§6 / §5, статус в §12
- `rejected` / `deferred` → только статус в §12

## Источники правды

- План: `docs/architecture/RAG_MIGRATION_PLAN.md`
- Состояние + RETRO: `docs/sprints/SPRINT_STATE.md`
