# Copy to deploy/prod/.env.migrate (не коммитить — содержит пароль источника)
# cp deploy/prod/.env.migrate.example deploy/prod/.env.migrate

# Удалённая RAG-база (источник)
SOURCE_DATABASE_URL=postgresql://avg:PASSWORD@83.166.253.250:5432/avg_dev

# Ожидаемая размерность embedding (должна совпадать с источником)
EMBEDDING_DIMS=1024

# Namespace для проверки после переноса (опционально)
# EXPECTED_NAMESPACE=bge-m3-v1
